import 'package:flutter/material.dart';

class SupportAndResources extends StatelessWidget {
  const SupportAndResources ({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('SupportAndResources'),
       
      ),
    );
  }
}